---
id: "birch-wake-linen"
created: "Thu Dec  2 15:45:28 2021"
global_id: "gCKs9N"
---



```{r bwl9-1, echo=FALSE, results="markup"}
askMC(
  "Which of the following R/mosaic functions takes an initial condition as one of the inputs?",
  "`antiD()`",
  "`D()`",
  "+`Iterate()`+"
)
```

```{r bwl9-2, echo=FALSE, results="markup"}
askMC(
  "Which of the following R/mosaic functions requires that you specify a domain  as one of the inputs to the function?",
  "`antiD()`",
  "`D()`",
  "`Iterate()`",
  "+`Solve()`+"
)
```

```{r bwl9-3, echo=FALSE, results="markup"}
askMC(
  "The R/mosaic functions `Solve()` and `argM()` return what kind of computer object?",
  "a number",
  "a function",
  "a graph",
  "+a data frame+"
)
```
